[tool.pytest.ini_options]
testpaths = [
    "sentinel/tests",
    "bulwark/tests",
    "covenant/tests",
    "vault/tests",
    "chronicle/tests",
    "paladin/tests",
]
addopts = "-v"

[tool.ruff]
target-version = "py311"
line-length = 100
